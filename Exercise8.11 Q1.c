// C Primier Plus Exercise8.11 Q1

#include<stdio.h>

int main(void)
{
	int ch; // use int instead of ch to compare with EOF
	
	while( (ch=getchar()) != EOF)
	putchar(ch);
	
	return 0;

 }
